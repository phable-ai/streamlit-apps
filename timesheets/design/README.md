# Original design handoff

`Weekly Timesheet.dc.html` (+ `support.js` to render it) is the Claude
Design canvas this app was built from. It's kept here for reference only —
it isn't used by the Streamlit app at runtime. Open the `.dc.html` file in
a browser to see the original interactive mockup and its exact copy/behavior.
